{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/manutej/stalks-and-sections/schemas/catalog.schema.json",
  "title": "SheafCatalog",
  "description": "Index of loadable SheafGraph files. Scrapers and the explorer both read this. The live app ALSO auto-globs docs/examples/*.json; this file is the explicit, scraper-facing list.",
  "type": "object",
  "required": ["schemaVersion", "graphs"],
  "properties": {
    "schemaVersion": { "type": "integer", "minimum": 1 },
    "graphs": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["id", "path"],
        "properties": {
          "id": { "type": "string" },
          "path": { "type": "string", "description": "Repo-relative path to a SheafGraph JSON." },
          "title": { "type": "string" },
          "kind": { "type": "string", "enum": ["codebase", "wiki", "triples", "demo", "literature"] },
          "ingest": { "type": "string", "description": "Optional LibraryIngest JSON that produced this graph." }
        }
      }
    }
  }
}
